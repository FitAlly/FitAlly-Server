spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fitally_test
    username: postgres
    password: "gurwls0177@"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: false

  data:
    redis:
      repositories:
        enabled: false

  social:
    apple:
      audience: test.apple.audience

jwt:
  secret: your-test-secret-key-your-test-secret-key-123456
  access-token-expiration: 3600000
  refresh-token-expiration: 1209600000